# 2025-11-28

## Project Configuration

- Initialized monorepo structure.
- Created `apps/server` using NestJS (v11).
- Created `apps/client` using Next.js (v16).
- Updated `GEMINI.md` to reflect the new stack (Next.js instead of Nuxt).
- Verified workspace linking with `pnpm install`.
